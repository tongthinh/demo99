import { SearchMinPricePipe } from './search-min-price.pipe';

describe('SearchMinPricePipe', () => {
  it('create an instance', () => {
    const pipe = new SearchMinPricePipe();
    expect(pipe).toBeTruthy();
  });
});
