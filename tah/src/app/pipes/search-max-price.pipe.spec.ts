import { SearchMaxPricePipe } from './search-max-price.pipe';

describe('SearchMaxPricePipe', () => {
  it('create an instance', () => {
    const pipe = new SearchMaxPricePipe();
    expect(pipe).toBeTruthy();
  });
});
